name: Test
on: [push]
jobs:
  Fetch-Commits:
    runs-on: [self-hosted, linux, x64]
    steps:
      - name: checkout branch
        uses: actions/checkout@v3
        with:
          ref: ElijahLynn-patch-1
          fetch-depth: 1
          
      - name: Push a new commit
        run: |
          dig jenkins.vfs.va.gov
